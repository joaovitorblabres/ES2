<%= form_with(model: high_score, local: true) do |form| %>
  <% if high_score.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(high_score.errors.count, "error") %> prohibited this high_score from being saved:</h2>

      <ul>
      <% high_score.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :idVoluntario %>
    <%= form.text_field :idVoluntario %>
  </div>

  <div class="field">
    <%= form.label :nome %>
    <%= form.text_field :nome %>
  </div>

  <div class="field">
    <%= form.label :informacao %>
    <%= form.text_area :informacao %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
